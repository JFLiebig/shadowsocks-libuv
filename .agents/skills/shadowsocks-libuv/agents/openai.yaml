interface:
  display_name: "Shadowsocks Libuv"
  short_description: "Repo-specific patterns and workflows for shadowsocks-libuv"
  default_prompt: "Use the shadowsocks-libuv repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true